import reservaNatural.*
const desierto = new Habitat()


test "la reserva"
{
	

assert.equals(reserva.habitatConMayorBiom(),0)
assert.equals(reserva.contieneEspecie(tigre))
assert.equals(reserva.habitatsNoEquilibrados(),0)
assert.equals(reserva.biomasaTotal(),0)

}

test "habitats" {
	
	assert.equals(desierto.biomasaHabitat(),5) 
	desierto.producirIncendio()
	assert.equals(desierto.biomasaHabitat(),5) 

	
}
